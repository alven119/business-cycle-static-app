Small official-format text fixtures for QA1E.2 parser tests.

These are minimal synthetic excerpts modeled on Census Advance Monthly Retail
Trade release wording. They are not full official artifacts and are used only
to test deterministic text-layer parsing, release-date extraction, and as-of
event timing without network access or OCR.
